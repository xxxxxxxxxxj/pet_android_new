<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/rl_pop_selectphoto_parent"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/transparent"
    tools:context="${relativePackage}.${activityClass}" >

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:alpha="0.8"
        android:background="@color/black" >
    </RelativeLayout>

    <ImageButton
        android:id="@+id/ib_pw_main_close"
        android:layout_width="31dip"
        android:layout_height="45.5dip"
        android:layout_marginTop="60dp"
        android:background="@drawable/main_pw_close_img"
        android:scaleType="fitXY"
        android:layout_alignParentTop="true"
        android:layout_alignRight="@+id/linearLayout"
        android:layout_alignEnd="@+id/linearLayout" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/ib_pw_main_close"
        android:layout_marginBottom="60dip"
        android:layout_marginLeft="20dip"
        android:layout_marginRight="20dip"
        android:background="@color/white"
        android:orientation="vertical"
        android:id="@+id/linearLayout">

        <TextView
            android:id="@+id/tv_pw_main_activitynum"
            android:layout_width="match_parent"
            android:layout_height="50dip"
            android:gravity="center"
            android:text="TextView"
            android:textColor="@color/a333333"
            android:textSize="14sp" />

        <ListView
            android:id="@+id/lv_pw_main"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_marginBottom="15dip"
            android:layout_marginLeft="15dip"
            android:layout_marginRight="15dip"
            android:divider="@color/white"
            android:dividerHeight="15dip"
            android:listSelector="@drawable/mlistview_selector"
            android:scrollbars="none" >
        </ListView>
    </LinearLayout>

</RelativeLayout>